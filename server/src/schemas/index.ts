// Import GraphQL type definitions and resolvers
import typeDefs from './typeDefs.js';
import resolvers from './resolvers.js';

// Export them together for Apollo Server to use
export { typeDefs, resolvers };